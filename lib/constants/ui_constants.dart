import 'package:flutter/material.dart';

const authInputFieldsContentPadding = EdgeInsets.only(
  top: 0,
  left: 10,
  bottom: 10,
);

const loginPageInputFieldTextStyle = TextStyle(
  color: Colors.pink,
);
